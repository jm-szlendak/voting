global:
  image:
    registryUrl: "szlendakjakub"
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  environment: "test"
  domainName: szlendak.ovh
redis: {}



postgresql:
  primary:
    initdb:
      scriptsConfigMap: postgres-init-script